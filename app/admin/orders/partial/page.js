'use client';
import { orders, formatCurrency, formatDate } from '@/lib/mock-data'; const partialOrders = orders.filter(o => o.status === 'partial'); export default function PartialReceiptPage() { return ( <div className="page-container"> <div className="page-header"> <div> <div className="page-title"> Ký nhận 1 phần</div> <div className="page-subtitle">Đơn hàng được nhận thiếu / không đủ số lượng</div> </div> </div> <div style={{ background: 'rgba(245,158,11,0.08)', border: '1px solid rgba(245,158,11,0.25)', borderRadius: 12, padding: '14px 18px', marginBottom: 24, display: 'flex', gap: 12, alignItems: 'center' }}> <span style={{ fontSize: 20 }}></span> <div> <div style={{ fontWeight: 600, color: '#fbbf24', fontSize: 14 }}>Lưu ý về ký nhận 1 phần</div> <div style={{ fontSize: 13, color: 'var(--text-muted)', marginTop: 2 }}>Các đơn này có sự chênh lệch giữa số lượng giao và số lượng khách nhận. Cần xác nhận với đơn vị vận chuyển để xử lý.</div> </div> </div> <div className="card" style={{ padding: 0 }}> <div className="table-wrapper"> <table className="table"> <thead><tr><th>Mã đơn</th><th>Khách hàng</th><th>Sản phẩm giao</th><th>Tổng giá trị</th><th>ĐVVC</th><th>Ghi chú</th><th>Ngày tạo</th><th>Hành động</th></tr></thead> <tbody> {partialOrders.length === 0 ? ( <tr><td colSpan={8}><div className="empty-state"><div className="empty-state-icon"></div><h3>Không có đơn nhận 1 phần</h3><p>Tất cả đơn đều được nhận đủ</p></div></td></tr> ) : partialOrders.map(order => ( <tr key={order.id}> <td><span style={{ color: 'var(--primary-light)', fontWeight: 700 }}>{order.id}</span></td> <td><div style={{ fontWeight: 600 }}>{order.customerName}</div><div style={{ fontSize: 12, color: 'var(--text-muted)' }}>{order.phone}</div></td> <td> {order.products.map((p, i) => ( <div key={i} style={{ fontSize: 13 }}>{p.name} <span style={{ color: '#fbbf24' }}>x{p.qty}</span></div> ))} </td> <td style={{ fontWeight: 700 }}>{formatCurrency(order.totalValue)}</td> <td><span className="badge" style={{ background: 'var(--bg-input)', color: 'var(--text-secondary)' }}>{order.shipper}</span></td> <td style={{ fontSize: 13, color: '#fbbf24' }}>{order.note}</td> <td style={{ fontSize: 12, color: 'var(--text-muted)' }}>{formatDate(order.createdAt)}</td> <td> <div style={{ display: 'flex', gap: 6 }}> <button className="btn btn-sm btn-success" style={{ padding: '4px 10px', fontSize: 12 }}>Xác nhận</button> <button className="btn btn-sm btn-danger" style={{ padding: '4px 10px', fontSize: 12 }}>Khiếu nại</button> </div> </td> </tr> ))} </tbody> </table> </div> </div> </div> );
}
