'use client';
import { orders, formatCurrency, formatDate, getStatusColor } from '@/lib/mock-data'; const shippingOrders = orders.filter(o => o.status === 'shipping'); export default function DeliveryPage() { return ( <div className="page-container"> <div className="page-header"> <div> <div className="page-title"> Đơn giao hàng</div> <div className="page-subtitle">{shippingOrders.length} đơn đang giao</div> </div> </div> <div className="grid-kpi" style={{ gridTemplateColumns: 'repeat(3, 1fr)', marginBottom: 24 }}> <div className="kpi-card"> <div className="kpi-icon kpi-icon-cyan"></div> <div className="kpi-content"><div className="kpi-value">{orders.filter(o=>o.status==='shipping').length}</div><div className="kpi-label">Đang giao</div></div> </div> <div className="kpi-card"> <div className="kpi-icon kpi-icon-yellow"></div> <div className="kpi-content"><div className="kpi-value">{orders.filter(o=>o.status==='pending').length}</div><div className="kpi-label">Chờ lấy hàng</div></div> </div> <div className="kpi-card"> <div className="kpi-icon kpi-icon-green"></div> <div className="kpi-content"><div className="kpi-value">{orders.filter(o=>o.status==='delivered').length}</div><div className="kpi-label">Đã giao thành công</div></div> </div> </div> <div className="card" style={{ padding: 0 }}> <div className="table-wrapper"> <table className="table"> <thead><tr><th>Mã đơn</th><th>Khách hàng</th><th>Địa chỉ</th><th>ĐVVC</th><th>Mã vận đơn</th><th>COD</th><th>Ngày tạo</th><th>Trạng thái</th></tr></thead> <tbody> {orders.filter(o => o.status === 'shipping' || o.status === 'pending').map(order => ( <tr key={order.id}> <td><span style={{ color: 'var(--primary-light)', fontWeight: 700 }}>{order.id}</span></td> <td><div style={{ fontWeight: 600 }}>{order.customerName}</div><div style={{ fontSize: 12, color: 'var(--text-muted)' }}>{order.phone}</div></td> <td style={{ fontSize: 12, color: 'var(--text-secondary)', maxWidth: 200 }}>{order.address}</td> <td><span className="badge" style={{ background: 'var(--bg-input)', color: 'var(--text-secondary)' }}>{order.shipper}</span></td> <td style={{ fontSize: 12, color: 'var(--text-muted)', fontFamily: 'monospace' }}>{order.trackingCode}</td> <td style={{ fontWeight: 700 }}>{formatCurrency(order.codAmount)}</td> <td style={{ fontSize: 12, color: 'var(--text-muted)' }}>{formatDate(order.createdAt)}</td> <td><span className={`badge ${getStatusColor(order.status)}`}>{order.status === 'shipping' ? 'Đang giao' : 'Chờ lấy hàng'}</span></td> </tr> ))} </tbody> </table> </div> </div> </div> );
}
