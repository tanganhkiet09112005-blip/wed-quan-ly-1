'use client';
import { blacklistCustomers, formatCurrency, formatDate } from '@/lib/mock-data'; export default function BlacklistPage() { return ( <div className="page-container"> <div className="page-header"> <div> <div className="page-title">Khách hàng bom hàng</div> <div className="page-subtitle">Danh sách đen các số điện thoại/địa chỉ có lịch sử xấu</div> </div> <button className="btn btn-primary" style={{ background: '#dc2626' }}> Thêm vào Blacklist </button> </div> <div style={{ background: '#fef2f2', border: '1px solid #fecaca', borderRadius: 10, padding: '14px 20px', marginBottom: 24 }}> <div style={{ fontWeight: 700, color: '#b91c1c', fontSize: 14, marginBottom: 4 }}>Hệ thống cảnh báo tự động</div> <div style={{ fontSize: 13, color: '#64748b' }}>Khi tạo đơn mới, hệ thống sẽ tự động đối chiếu SĐT và địa chỉ với danh sách này để cảnh báo bạn trước khi giao hàng.</div> </div> <div className="card" style={{ padding: 0 }}> <div className="table-wrapper"> <table className="table"> <thead> <tr> <th>Khách hàng</th> <th>SĐT (Cảnh báo chính)</th> <th>Địa chỉ</th> <th>Lý do</th> <th>Lịch sử bom</th> <th>Giá trị thiệt hại</th> <th>Ngày thêm</th> <th>Mức độ</th> </tr> </thead> <tbody> {blacklistCustomers.map(customer => ( <tr key={customer.id}> <td style={{ fontWeight: 600 }}>{customer.name}</td> <td><span style={{ fontFamily: 'monospace', fontWeight: 700, color: '#dc2626', background: '#fef2f2', padding: '2px 8px', borderRadius: 4, border: '1px solid #fecaca' }}>{customer.phone}</span></td> <td style={{ fontSize: 13, color: '#475569', maxWidth: 200 }}>{customer.address}</td> <td style={{ fontSize: 13, color: '#dc2626', maxWidth: 150 }}>{customer.reason}</td> <td style={{ fontWeight: 600, textAlign: 'center' }}>{customer.orderCount} lần</td> <td style={{ fontWeight: 700 }}>{formatCurrency(customer.totalValue)}</td> <td style={{ fontSize: 12, color: '#64748b' }}>{formatDate(customer.addedAt)}</td> <td> {customer.severity === 'high' ? ( <span style={{ display: 'inline-block', padding: '3px 10px', borderRadius: 4, fontSize: 12, fontWeight: 600, background: '#fef2f2', color: '#991b1b', border: '1px solid #fecaca' }}>Cao</span> ) : ( <span style={{ display: 'inline-block', padding: '3px 10px', borderRadius: 4, fontSize: 12, fontWeight: 600, background: '#fffbeb', color: '#92400e', border: '1px solid #fde68a' }}>Trung bình</span> )} </td> </tr> ))} </tbody> </table> </div> </div> </div> );
}
