'use client';
import { useState, useEffect } from 'react';
import Link from 'next/link';
import { formatCurrency, getStatusLabel, getStatusColor } from '@/lib/mock-data'; const ALL_STATUSES = ['all', 'pending', 'shipping', 'delivered', 'returned', 'cancelled', 'issue', 'partial'];
const STATUS_LABELS = { all: 'Tất cả', pending: 'Chờ xử lý', shipping: 'Đang giao', delivered: 'Đã giao', returned: 'Trả hàng', cancelled: 'Đã hủy', issue: 'Kiện vấn đề', partial: 'Nhận 1 phần'
}; export default function ManageOrdersPage() { const [orders, setOrders] = useState([]); const [loading, setLoading] = useState(true); const [search, setSearch] = useState(''); const [statusFilter, setStatusFilter] = useState('all'); const [channelFilter, setChannelFilter] = useState('all'); const [shipperFilter, setShipperFilter] = useState('all'); useEffect(() => { fetch('/api/orders') .then(res => res.json()) .then(data => { if (data.success) { setOrders(data.data); } setLoading(false); }) .catch(err => { console.error(err); setLoading(false); }); }, []); const handleStatusUpdate = async (id, currentStatus) => { let nextStatus = currentStatus; if (currentStatus === 'pending') nextStatus = 'shipping'; else if (currentStatus === 'shipping') nextStatus = 'delivered'; else return; try { const res = await fetch(`/api/orders/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status: nextStatus }) }); if (res.ok) { setOrders(orders.map(o => o.id === id ? { ...o, status: nextStatus } : o)); } } catch (err) { console.error(err); } }; const filtered = orders.filter(o => { const matchSearch = !search || o.code?.includes(search) || o.shippingName?.toLowerCase().includes(search.toLowerCase()) || o.shippingPhone?.includes(search); const matchStatus = statusFilter === 'all' || o.status === statusFilter; const matchChannel = channelFilter === 'all' || o.channel === channelFilter; const matchShipper = shipperFilter === 'all' || o.shipperCode === shipperFilter; return matchSearch && matchStatus && matchChannel && matchShipper; }); if (loading) { return ( <div className="page-container" style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '60vh' }}> <div style={{ fontSize: 18, color: 'var(--text-muted)' }}>Đang tải dữ liệu...</div> </div> ); } return ( <div className="page-container"> <div className="page-header"> <div> <div className="page-title"> Quản lý đơn hàng</div> <div className="page-subtitle">{filtered.length} đơn hàng từ CSDL</div> </div> <Link href="/admin/orders/create" className="btn btn-primary"> Tạo đơn mới</Link> </div> {/* Status tabs */} <div style={{ display: 'flex', gap: 4, overflowX: 'auto', marginBottom: 16, paddingBottom: 4 }}> {ALL_STATUSES.map(s => ( <button key={s} onClick={() => setStatusFilter(s)} style={{ padding: '8px 16px', borderRadius: 20, fontSize: 13, fontWeight: 600, border: 'none', cursor: 'pointer', whiteSpace: 'nowrap', transition: 'var(--transition)', background: statusFilter === s ? 'var(--primary)' : 'var(--bg-card)', color: statusFilter === s ? 'white' : 'var(--text-secondary)', boxShadow: statusFilter === s ? '0 4px 12px rgba(99,102,241,0.3)' : 'none', }}> {STATUS_LABELS[s]} <span style={{ marginLeft: 6, opacity: 0.7, fontSize: 11 }}> {s === 'all' ? orders.length : orders.filter(o => o.status === s).length} </span> </button> ))} </div> {/* Filter bar */} <div className="filter-bar"> <div className="search-wrapper"> <span className="search-icon"></span> <input className="search-input" placeholder="Tìm mã đơn, khách hàng, SĐT..." value={search} onChange={e => setSearch(e.target.value)} /> </div> <select className="form-control" style={{ width: 160 }} value={channelFilter} onChange={e => setChannelFilter(e.target.value)}> <option value="all">Tất cả kênh</option> <option value="direct">Trực tiếp</option> <option value="fanpage">Fanpage</option> <option value="livestream">Livestream</option> </select> <select className="form-control" style={{ width: 140 }} value={shipperFilter} onChange={e => setShipperFilter(e.target.value)}> <option value="all">Tất cả ĐVVC</option> <option value="GHN">GHN</option> <option value="SPX">SPX</option> <option value="JT">J&T</option> </select> </div> <div className="card" style={{ padding: 0 }}> <div className="table-wrapper"> <table className="table"> <thead> <tr> <th><input type="checkbox" /></th> <th>Mã đơn</th> <th>Khách hàng</th> <th>Sản phẩm</th> <th>Giá trị COD</th> <th>ĐVVC</th> <th>Kênh</th> <th>Ngày tạo</th> <th>Trạng thái</th> <th>Thao tác</th> </tr> </thead> <tbody> {filtered.length === 0 ? ( <tr> <td colSpan={10}> <div className="empty-state"> <div className="empty-state-icon"></div> <h3>Không có đơn hàng</h3> <p>Thử thay đổi bộ lọc hoặc tạo đơn mới</p> </div> </td> </tr> ) : filtered.map(order => ( <tr key={order.id}> <td><input type="checkbox" /></td> <td> <span style={{ color: 'var(--primary-light)', fontWeight: 700, fontSize: 13 }}>{order.code}</span> {order.note && <div style={{ fontSize: 11, color: 'var(--text-muted)', maxWidth: 120, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }} title={order.note}> {order.note}</div>} </td> <td> <div style={{ fontWeight: 600 }}>{order.shippingName}</div> <div style={{ fontSize: 12, color: 'var(--text-muted)' }}>{order.shippingPhone}</div> </td> <td style={{ maxWidth: 180 }}> <div style={{ fontSize: 13, color: 'var(--text-secondary)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}> {order.items?.map(p => `${p.name} x${p.quantity}`).join(', ')} </div> </td> <td><span style={{ fontWeight: 700 }}>{formatCurrency(order.codAmount)}</span></td> <td><span className="badge" style={{ background: 'var(--bg-input)', color: 'var(--text-secondary)' }}>{order.shipperCode || '—'}</span></td> <td> <span style={{ fontSize: 12 }}> {order.channel === 'fanpage' ? ' Fanpage' : order.channel === 'livestream' ? ' Livestream' : ' Trực tiếp'} </span> </td> <td style={{ fontSize: 12, color: 'var(--text-muted)' }}>{new Date(order.createdAt).toLocaleDateString('vi-VN')}</td> <td><span className={`badge ${getStatusColor(order.status)}`}>{getStatusLabel(order.status)}</span></td> <td> <div style={{ display: 'flex', gap: 6 }}> {(order.status === 'pending' || order.status === 'shipping') && ( <button onClick={() => handleStatusUpdate(order.id, order.status)} className="btn btn-primary btn-sm" style={{ padding: '4px 10px', fontSize: 12 }}> {order.status === 'pending' ? 'Giao hàng' : 'Hoàn thành'} </button> )} <button className="btn btn-secondary btn-sm" style={{ padding: '4px 10px', fontSize: 12 }}></button> </div> </td> </tr> ))} </tbody> </table> </div> </div> </div> );
}
